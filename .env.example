# App
PORT=3000

# Database
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/trademesh?schema=public"

# Auth
JWT_SECRET="replace_with_a_strong_secret"

# Mail (SMTP / Nodemailer)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your_smtp_username"
SMTP_PASS="your_smtp_password"
MAIL_FROM="TradeMesh <no-reply@example.com>"
